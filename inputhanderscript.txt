using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//inputhander
public class InputHander : MonoBehaviour
{
    [SerializeField] BirdMover _birdMover;

    private InputScheme _inputs;    

    private void Awake()
    { 
        if (_birdMover == null)
            enabled = false

        _inputs = new InputScheme()

        _inputs.ActionMap.Touch.performed += 
    }

    private  void OnEnable()
    {
        _inputs.ActionMap.Touch.performed += OnTouch
    }

    private void OnDisable()
    {
        _inputs.ActionMap.Touch.performed += OnTouch
    }
    private void OnTouch(CallbackCOntext context)
    {


    }
}
